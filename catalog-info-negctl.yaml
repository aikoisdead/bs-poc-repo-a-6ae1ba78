apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: poc-negctl-api
  description: NEGATIVE CONTROL - $openapi bundling a $ref into a repo the integration token cannot read
spec:
  type: openapi
  lifecycle: experimental
  owner: guests
  definition:
    $openapi: ./openapi-negctl.yaml
